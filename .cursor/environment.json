{
  "name": "BelowGradePros",
  "install": "npm install && npx prisma migrate deploy",
  "start": "npm run dev -- --hostname 0.0.0.0 --port 3000",
  "terminals": [
    {
      "name": "dev-server",
      "command": "npm run dev -- --hostname 0.0.0.0 --port 3000"
    }
  ],
  "ports": [
    {
      "name": "Next.js",
      "port": 3000
    }
  ]
}


services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: belowgradepros
    ports:
      - "5432:5432"
    volumes:
      - belowgradepros_pg:/var/lib/postgresql/data

volumes:
  belowgradepros_pg:
